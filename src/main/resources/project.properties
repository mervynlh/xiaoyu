#db
jdbc.driverClassName=${m.jdbc.driverClassName}
jdbc.url=${m.jdbc.url}
jdbc.username=${m.jdbc.username}
jdbc.password=${m.jdbc.password}
jdbc.maxPoolSize=${m.jdbc.maxPoolSize}
jdbc.minPoolSize=${m.jdbc.minPoolSize}
jdbc.initialPoolSize=${m.jdbc.initialPoolSize}
jdbc.idleConnectionTestPeriod=1800
jdbc.maxIdleTime=3600

#memcach start
server1=${m.server1}
server2=${m.server2}
opTimeout=3
opTimeoutBulk=3
retry=1
readBufSize=16384
opQueueLen=16384
expHour=24
#0 不使用memcache 1使用memcache
isUse=${m.isUse}
#memcach end


#项目路径
contextPath=${m.contextPath}
#直播视频api地址
zoomMeetingAPIUrl=${m.zoomMeetingApi}
#图片、CSS、js静态资源文件地址
staticServer = ${m.staticServer}

#首页评论文件
assessFile=${m.assessFile}

#上传服务用服务器地址，访问时用staticImage，数据库中不存储域名
uploadImageServer=${m.uploadImageServer}
staticImage=${m.staticImage}
projectName=${m.projectName}

#lucene路径配置
luceneIndexDir=${m.luceneIndexDir}
luceneIndexDirBak=${m.luceneIndexDirBak}
alertemail=${m.alertemail}

# user relogin
email.host=${m.email.host}
email.username=${m.email.username}
email.password=${m.email.password}

mydomain=${m.mydomain}

#短信接口
msgurl=${m.msgurl}
acountSid=${m.acountSid}
authToken=${m.authToken}
appId=${m.appId}

#亲加通信云即时通讯开发者帐号配置
im.email=${m.im.email}
im.devpwd=${m.im.devpwd}
im.appkey=${m.im.appkey}

